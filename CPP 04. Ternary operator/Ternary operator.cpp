#include<iostream>

using namespace std;

int main() {
	// ?: -  ternary operator -> bool_return_expression ? if_true : if_false;

#pragma region ternary operator example
/*int number{ };
	cin >> number;*/
	/*if (number % 2 == 0) {
		cout << "Cut ededdir" << endl;
	}
	else {
		cout << "Tek ededdir" << endl;
	}*/

	/*if (number % 2 == 0) cout << "Cut ededdir" << endl;
	else cout << "Tek ededdir" << endl;*/

	/* number % 2 == 0 ? cout << "Cut ededdir" << endl : cout << "Tek ededdir" << endl;*/

	/*cout << (number % 2 == 0 ?  "Cut ededdir"  : "Tek ededdir") << endl;*/
	/*int absNumber{};*/
	/*if (number >= 0) absNumber = number;
	else absNumber = -number;*/

	/*absNumber = number >= 0 ? number : -number;
	cout <<"|" << number << "| = " << absNumber << endl;*/
#pragma endregion

#pragma region nested ternary -  not good
	/*
	int numb{};
	cin >> numb;
	cout << ( numb > 0 ? "musbet" : numb < 0 ? "menfi" : "sifir") << endl;
	*/
#pragma endregion

	
}